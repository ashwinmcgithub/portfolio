# Portfolio Project Memory

## Stack
- Next.js 14 + TypeScript + Tailwind CSS (App Router)
- Components: /components/ui/ (shadcn structure)
- 3D: Spline for hero, framer-motion for animations

## C Drive FULL — Build Commands
- npm cache: D:\npm-cache (set in ~/.npmrc)
- Build: TEMP="/d/tmp" TMPDIR="/d/tmp" LOCALAPPDATA="D:\\AppData\\Local" npm run build
- Use Next.js 14.x (16.x WASM crashes), next.config.mjs not .ts

## Key Files
- app/page.tsx: Main page (SplineHero + projects grid)
- components/ui/spline-hero.tsx: Hero component
- components/ui/splite.tsx: Spline lazy loader
- netlify.toml: build="npm run build" publish=".next"

## 2026-03-10 Card Refresh Log
- Replaced the old emoji-based project cards in `components/ui/projects-section.tsx` with a reusable animated card component in `components/ui/schema-card-with-animated-wave-visualizer.tsx`.
- Added `components/ui/demo.tsx` as a direct usage example of the new schema card import pattern from the integration brief.
- Tailwind setup is Tailwind CSS 3 with App Router globals in `app/globals.css`, so animation utilities and card surface classes were added there instead of using Tailwind 4 `index.css` imports.
- Installed `lucide-react` with `npm.cmd install lucide-react --legacy-peer-deps` because the repo has an existing `eslint` / `eslint-config-next` peer conflict unrelated to the UI work.
- Demo and completed project cards now use Lucide icons plus Unsplash image URLs for the visual layer; if Claude adjusts the design again, update the project data objects in `components/ui/projects-section.tsx` first.
